<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
			 xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
			 xmlns:pancake="clr-namespace:Xamarin.Forms.PancakeView;assembly=Xamarin.Forms.PancakeView"
			 x:Class="Listup.Views.HomePage"
             x:Name="RootPage"
             xmlns:converters="clr-namespace:Listup.Views.Converters"
			 Style="{StaticResource GlobalPrimaryPageBackgroundStyle}">

    <ContentPage.Resources>
        <converters:BooleanNegationConverter x:Key="BooleanNegation" />
    </ContentPage.Resources>
    
    <StackLayout>
        <StackLayout IsVisible="{Binding IsLoading, Converter={StaticResource BooleanNegation}}">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Frame Grid.Row="0" Style="{StaticResource GlobalHeader}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <Label Text="LISTUP - Carrinhos e listas" Style="{StaticResource GlobalTitleLabel}" Grid.Column="0" LineBreakMode="WordWrap" />
                        <Label Text="&#xf828;" FontFamily="FontAwesomeSolid" Grid.Column="1" Style="{StaticResource GlobalIconHeader}">
                            <Label.GestureRecognizers>
                                <TapGestureRecognizer Tapped="OnOrderByLabelTapped" />
                            </Label.GestureRecognizers>
                        </Label>

                        <Label Text="&#xf013;" FontFamily="FontAwesomeSolid" Grid.Column="2" Style="{StaticResource GlobalIconHeader}" >
                            <Label.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding OpenSettingsPageCommand}" />
                            </Label.GestureRecognizers>
                        </Label>
                    </Grid>
                </Frame>

                <Label Grid.Row="1" IsVisible="{Binding HasCarts, Converter={StaticResource BooleanNegation}}" Text="Não há nenhuma compra criada.&#10;Crie uma nova clicando em&#10;“＋ Nova compra”" Style="{DynamicResource NoCartsTitle}" />
                
                <ScrollView Grid.Row="1">
                    <StackLayout>
                        <CollectionView ItemsSource="{Binding Carts}">
                            <CollectionView.ItemTemplate>
                                <DataTemplate>
                                    <SwipeView>
                                        <SwipeView.RightItems>
                                            <SwipeItems Mode="Reveal">
                                                <SwipeItem
                                                Text="Excluir"
                                                BackgroundColor="{StaticResource TertiaryColor}"
                                                Command="{Binding BindingContext.DeleteCartCommand, Source={x:Reference RootPage}}" CommandParameter="{Binding .}" />
                                            </SwipeItems>
                                        </SwipeView.RightItems>
                                        <SwipeView.LeftItems>
                                            <SwipeItems Mode="Reveal">
                                                <SwipeItem
                                                Text="Detalhes"
                                                BackgroundColor="{StaticResource MainColor}"
                                                Command="{Binding BindingContext.DetailsCartCommand, Source={x:Reference RootPage}}" CommandParameter="{Binding .}" />
                                            </SwipeItems>
                                        </SwipeView.LeftItems>

                                        <Frame Style="{StaticResource CartContainer}">
                                            <Frame.GestureRecognizers>
                                                <TapGestureRecognizer Command="{Binding BindingContext.OpenCartScreenCommand, Source={x:Reference RootPage}}" CommandParameter="{Binding .}" />
                                            </Frame.GestureRecognizers>
                                            <Grid ColumnDefinitions="1*,3*" ColumnSpacing="0" VerticalOptions="FillAndExpand" HorizontalOptions="Center" Padding="0">
                                                <Grid BackgroundColor="Transparent" Grid.Column="0" VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand">
                                                    <StackLayout VerticalOptions="Center">
                                                        <Label Text="&#xf0ae;" Style="{StaticResource CartContainerIcon}" />
                                                    </StackLayout>
                                                </Grid>

                                                <Grid BackgroundColor="Transparent" Grid.Column="1" VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand">
                                                    <StackLayout>
                                                        <Label Text="{Binding Title}" Style="{StaticResource CartContainerTitle}" />
                                                        <StackLayout Orientation="Horizontal">
                                                            <Label Text="{Binding CreationDate, StringFormat='Criado em: {0:dd/MM/yyyy}'}" Style="{StaticResource CartDates}" />
                                                            <Label Text="{Binding EditedDate, StringFormat='Editado em: {0:dd/MM/yyyy}'}" Style="{StaticResource CartDates}" />
                                                        </StackLayout>
                                                        <Label Text="{Binding Description}" Style="{StaticResource CartDescription}" />
                                                    </StackLayout>
                                                </Grid>
                                            </Grid>
                                        </Frame>
                                    </SwipeView>
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                        </CollectionView>
                    </StackLayout>
                </ScrollView>

                <Grid Grid.Row="2">
                    <StackLayout>
                        <Label Text="Arraste um item para excluí-lo ou ver detalhes" IsVisible="{Binding HasCarts}" Style="{StaticResource LblHowToDelete}" />
                        <Frame Style="{StaticResource ExternalBottomContainer}">
                            <pancake:PancakeView Style="{StaticResource GlobalInternalBottomContainer}">
                                <Button Text="＋ Nova compra" Command="{Binding AddNewCartCommand}" Style="{StaticResource GlobalPrimaryButton}" />
                            </pancake:PancakeView>
                        </Frame>
                    </StackLayout>
                </Grid>
            </Grid>
        </StackLayout>
        <StackLayout IsVisible="{Binding IsLoading}" Style="{StaticResource StackLayoutLoadIndicator}">
            <Frame Style="{StaticResource ContainerLoadIndicator}">
            <ActivityIndicator IsRunning="true" Style="{StaticResource LoadIndicator}" />
            </Frame>
        </StackLayout>
    </StackLayout>
</ContentPage>