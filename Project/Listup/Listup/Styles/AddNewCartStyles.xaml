<?xml version="1.0" encoding="utf-8" ?>
<ResourceDictionary xmlns="http://xamarin.com/schemas/2014/forms"
                    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                    xmlns:pancake="clr-namespace:Xamarin.Forms.PancakeView;assembly=Xamarin.Forms.PancakeView"
                    xmlns:toolkit="http://xamarin.com/schemas/2020/toolkit"
                    x:Class="Listup.Styles.AddNewCartStyles">

    <Style TargetType="Frame" x:Key="FrameCloseScreen">
        <Setter Property="BackgroundColor" Value="Transparent" />
    </Style>

    <Style x:Key="ActivityIndicatorContainer" TargetType="Frame">
        <Setter Property="HeightRequest" Value="50" />
        <Setter Property="WidthRequest" Value="50" />
        <Setter Property="Padding" Value="20" />
        <Setter Property="BackgroundColor" Value="{DynamicResource MainContainerColor}" />
        <Setter Property="HasShadow" Value="False" />
        <Setter Property="CornerRadius" Value="30" />
        <Setter Property="VerticalOptions" Value="Center" />
        <Setter Property="HorizontalOptions" Value="Center" />
    </Style>

    <Style x:Key="ActivityIndicator" TargetType="ActivityIndicator">
        <Setter Property="Color" Value="{DynamicResource MainColor}" />
        <Setter Property="WidthRequest" Value="25" />
        <Setter Property="HeightRequest" Value="25" />
    </Style>

    <Style x:Key="ContentConteinerGrid" TargetType="Grid">
        <Setter Property="VerticalOptions" Value="End" />
        <Setter Property="HorizontalOptions" Value="FillAndExpand" />
        <Setter Property="Margin" Value="0" />
        <Setter Property="Padding" Value="0" />
    </Style>

    <Style TargetType="Frame" x:Key="ExternalBottomContainer" BasedOn="{StaticResource GlobalExternalBottomContainer}">
        <Setter Property="HeightRequest" Value="320" />
    </Style>

    <Style TargetType="Label" x:Key="TitleLabel">
        <Setter Property="TextColor" Value="{DynamicResource MainColor}" />
        <Setter Property="FontAttributes" Value="Bold" />
        <Setter Property="Margin" Value="0,15,0,0" />
        <Setter Property="FontSize" Value="20" />
        <Setter Property="HorizontalOptions" Value="Center" />
    </Style>

    <Style TargetType="Frame" x:Key="OptionContainer">
        <Setter Property="BackgroundColor" Value="{DynamicResource SecondaryContainerColor}" />
        <Setter Property="CornerRadius" Value="5" />
        <Setter Property="HorizontalOptions" Value="FillAndExpand" />
        <Setter Property="Margin" Value="30,10,30,0" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="HeightRequest" Value="90" />
    </Style>

    <Style TargetType="Label" x:Key="OptionLabelDescription">
        <Setter Property="TextColor" Value="{DynamicResource MainTextColor }"/>
        <Setter Property="FontAttributes" Value="Bold" />
        <Setter Property="FontSize" Value="15" />
        <Setter Property="HorizontalTextAlignment" Value="Center" />
        <Setter Property="Margin" Value="0,4,0,0" />
    </Style>

    <Style TargetType="Label" x:Key="OptionIcon">
        <Setter Property="FontSize" Value="35" />
        <Setter Property="TextColor" Value="{DynamicResource FontIconsColorPrimary}" />
        <Setter Property="HorizontalOptions" Value="Center" />
        <Setter Property="FontFamily" Value="FontAwesomeSolid" />
    </Style>

    <Style TargetType="Label" x:Key="OptionTitle">
        <Setter Property="FontSize" Value="18" />
        <Setter Property="FontAttributes" Value="Bold" />
        <Setter Property="TextColor" Value="{DynamicResource MainTextColor }" />
        <Setter Property="HorizontalOptions" Value="Center" />
    </Style>

    <Style TargetType="StackLayout" x:Key="OptionInternalContainersStackLayout">
        <Setter Property="Orientation" Value="Vertical" />
        <Setter Property="VerticalOptions" Value="Center" />
        <Setter Property="Margin" Value="5,0,0,0" />
    </Style>

</ResourceDictionary>